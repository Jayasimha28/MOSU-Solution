apiVersion: v1
kind: Service
metadata:
  labels:
    app: web
  name: web-node
  namespace: net-lab
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
    nodePort: 30080
  selector:
    app: web
  type: NodePort
status:
  loadBalancer: {}
