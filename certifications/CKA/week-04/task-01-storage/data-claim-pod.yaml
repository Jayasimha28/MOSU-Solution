apiVersion: v1
kind: Pod
metadata:
  name: mypod
  namespace: storage-lab
spec:
  containers:
    - name: myfrontend
      image: nginx
      volumeMounts:
      - mountPath: /data
        name: data-claim
  volumes:
    - name: data-claim
      persistentVolumeClaim:
        claimName: data-claim
